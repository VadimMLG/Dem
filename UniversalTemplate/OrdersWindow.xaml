<Window x:Class="UniversalApp.OrdersWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Заказы"
 Height="700"
 Width="1200"
 FontFamily="Times New Roman"
 Background="#FFFFFF"
 Icon="/resources/Icon.ico"
 WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Список заказов" FontSize="24" FontWeight="Bold"
VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
Margin="20,0,0,0">
                <Button x:Name="AddOrderButton" Content="Добавить заказ"
Width="160" Height="34" Margin="0,0,8,0" Background="#4DA8DA"
Click="AddOrder_Click"/>
                <Button x:Name="EditOrderButton" Content="Редактировать заказ"
Width="180" Height="34" Margin="0,0,8,0" Background="#1273DE"
Click="EditOrder_Click"/>
                <Button x:Name="DeleteOrderButton" Content="Удалить заказ"
Width="160" Height="34" Margin="0,0,8,0" Background="#1273DE"
Click="DeleteOrder_Click"/>
                <Button Content="Назад" Width="120" Height="34"
Background="#1273DE" Click="Back_Click"/>
            </StackPanel>
        </StackPanel>
        <DataGrid Grid.Row="1"
 x:Name="OrdersGrid"
 AutoGenerateColumns="False"
 IsReadOnly="True"
 HeadersVisibility="Column"
 MouseDoubleClick="OrdersGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding
OrderNumber}" Width="110"/>
                <DataGridTextColumn Header="Артикул заказа" Binding="{Binding
ArticlesText}" Width="260"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding
StatusName}" Width="170"/>
                <DataGridTextColumn Header="Пункт выдачи" Binding="{Binding
PickupAddress}" Width="290"/>
                <DataGridTextColumn Header="Дата заказа" Binding="{Binding
OrderDate, StringFormat={}{0:yyyy-MM-dd}}" Width="140"/>
                <DataGridTextColumn Header="Дата выдачи" Binding="{Binding
DeliveryDate, StringFormat={}{0:yyyy-MM-dd}}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
